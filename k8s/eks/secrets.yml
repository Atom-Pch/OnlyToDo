apiVersion: v1
kind: Secret
metadata:
  name: todo-secrets
  namespace: default
type: Opaque
stringData:
  # Replace these with your actual secure production values!
  db_pass: ${DB_PASS}
  jwt_token: ${JWT_TOKEN}
