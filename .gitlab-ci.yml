# This is just a scheduler for Github Actions
stages:
  - infra_create
  - infra_destroy

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "web"
    - if: $CI_PIPELINE_SOURCE == "schedule"

variables:
  GITHUB_USER: "Atom-Pch"
  GITHUB_REPO: "TodoWebApp"

morning_infra_create:
  stage: infra_create
  image: alpine/curl:8.17.0
  rules:
    - if: $CI_PIPELINE_SOURCE == "web" && $ACTION == "up"
    - if: $CI_PIPELINE_SOURCE == "schedule" && $ACTION == "up"
  script: |
    echo "Triggering GitHub Actions: Morning Startup..."
    curl -L \
      -X POST \
      -H "Accept: application/vnd.github+json" \
      -H "Authorization: Bearer $GITHUB_PAT" \
      -H "X-GitHub-Api-Version: 2026-03-10" \
      https://api.github.com/repos/$GITHUB_USER/$GITHUB_REPO/actions/workflows/infra_up.yml/dispatches \
      -d '{"ref":"main"}'

evening_infra_destroy:
  stage: infra_destroy
  image: alpine/curl:8.17.0
  rules:
    - if: $CI_PIPELINE_SOURCE == "web" && $ACTION == "down"
    - if: $CI_PIPELINE_SOURCE == "schedule" && $ACTION == "down"
  script: |
    echo "Triggering GitHub Actions: Evening Teardown..."
    curl -L \
       -X POST \
       -H "Accept: application/vnd.github+json" \
       -H "Authorization: Bearer $GITHUB_PAT" \
       -H "X-GitHub-Api-Version: 2026-03-10" \
       https://api.github.com/repos/$GITHUB_USER/$GITHUB_REPO/actions/workflows/infra_down.yml/dispatches \
       -d '{"ref":"main"}'
